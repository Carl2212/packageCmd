import { AlertController, NavController, NavParams, ViewController } from 'ionic-angular';
import { ProfileService } from "../../../core/providers/profile/profile.service";
import { CoreStore } from "../../../core/core.store";
import { TranslateService } from "@ngx-translate/core";
import { ToastService } from "../../../core/providers/toast.service";
import { ProfileStore } from "../../../core/providers/profile/profile.store";
import { LoadingService } from "../../../core/providers/loading.service";
export declare class ProfileEdit {
    private navCtrl;
    private navParams;
    private store;
    pfStore: ProfileStore;
    private pfService;
    private tlService;
    private tsService;
    private alertCtrl;
    private viewCtrl;
    private loading;
    private editStatus;
    private oldProfile;
    private existCompanyRole;
    private profileNameGroup;
    name: any;
    constructor(navCtrl: NavController, navParams: NavParams, store: CoreStore, pfStore: ProfileStore, pfService: ProfileService, tlService: TranslateService, tsService: ToastService, alertCtrl: AlertController, viewCtrl: ViewController, loading: LoadingService);
    init(): void;
    getProfileNameGroup(): void;
    ionViewWillLeave(): void;
    selectCompany(): Promise<void>;
    save(): void;
    pop(): void;
    del(): void;
}

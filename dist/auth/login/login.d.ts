import { FormBuilder, FormGroup } from '@angular/forms';
import { NavController } from 'ionic-angular';
import { TranslateService } from "@ngx-translate/core";
import { AuthService } from '../auth.service';
import { ToastService } from '../../core/providers/toast.service';
import { LoadingService } from '../../core/providers/loading.service';
import { CoreStore } from "../../core/core.store";
import { ProfileService } from "../../core/providers/profile/profile.service";
export declare class Login {
    tlService: TranslateService;
    private formBuilder;
    private navCtrl;
    private authService;
    private tsService;
    private loading;
    private coreStore;
    profileService: ProfileService;
    langList: Array<any>;
    user: any;
    language: string;
    loginForm: FormGroup;
    username: any;
    password: any;
    constructor(tlService: TranslateService, formBuilder: FormBuilder, navCtrl: NavController, authService: AuthService, tsService: ToastService, loading: LoadingService, coreStore: CoreStore, profileService: ProfileService);
    private initForm();
    private initLoginInfo();
    langChange(e: string): void;
    login(): void;
    loginWithProfile(): void;
    popToServer(): void;
    private setLangList(data);
    ngOnInit(): void;
}
